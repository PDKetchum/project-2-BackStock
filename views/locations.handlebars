<section class="centered-form">
  <div class="container">
    <div class="row row-cols-auto">
      {{#each locations as |location|}}
      {{> location-details}}
      {{/each}}
    </div>
  </div>
</section>

<script src="/public/js/locations-page.js"></script>